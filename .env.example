DATABASE_URL=postgresql+asyncpg://opensoar:changeme@localhost:5432/opensoar
REDIS_URL=redis://localhost:6379/0
PLAYBOOK_DIRS=playbooks
API_KEY_SECRET=changeme
JWT_SECRET=changeme

# Docker Compose (optional — defaults are fine for local dev)
POSTGRES_PASSWORD=changeme
ELASTIC_PASSWORD=changeme
KIBANA_PASSWORD=changeme
KIBANA_ENCRYPTION_KEY=changeme-32-char-encryption-key!
